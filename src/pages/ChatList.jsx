import { useState, useEffect, useContext } from "react";
import { allChats, getChat, createChat, deleteChat } from "../services/ChatService";

import "./ChatList.css"

const ChatList = () => {
    return (
        <div>
            
        </div>
    );
};

export default ChatList;